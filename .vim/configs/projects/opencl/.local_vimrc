" Tab size {{{
set tabstop=2
set shiftwidth=2
set expandtab
" }}} Tab size
" Generate YCM index file {{{ "
"  -extraopts="-DCMAKE_EXPORT_COMPILE_COMMANDS=ON"
" }}} Generate YCM index file "
" Build options {{{ "
let s:root = expand('<sfile>:h').'/'
let &makeprg="cat ".s:root."outputfile.log"
let mprg="python ".s:root."automation/Build.py -t Ubuntu64 -b Debug --no-color"
" }}} Build options "
